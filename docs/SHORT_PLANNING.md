# Краткосрочный план разработки Yoga Mini App

## Логические основания проекта
- **Вещи**: Telegram Mini App для йоги и медитаций с системой квиза для подбора практик
- **Свойства**: Интерактивность, персонализация, доступность, интеграция с Telegram
- **Отношения**: Пользователь ↔ Практики, Выбор пользователя ↔ Рекомендации, Телеграм ↔ Приложение

## Приоритеты текущего спринта
1. Разработка системы квиза для подбора практик (первая ключевая фича)
2. Создание необходимых таблиц в Supabase и настройка связей
3. Реализация базовой админ-панели для управления контентом и квизом
4. Разработка компонентов плееров для различных типов контента

## Текущий статус
- Базовый шаблон приложения с авторизацией Telegram
- Интеграция с Supabase (таблица users)
- Основные компоненты навигации и интерфейса

## План действий по квизу

### 1. Создание структуры базы данных (🔴 Не начато)
- Создать таблицу `quiz_logic` для хранения правил подбора практик
- Создать таблицу `user_quiz_history` для сохранения истории выборов
- Создать таблицу `contents` для хранения практик
- Создать таблицу `user_stats` для статистики пользователя
- Настроить связи между таблицами
- Определить политики безопасности (RLS)

### 2. Разработка интерфейса квиза (🔴 Не начато)
- Создать компоненты для каждого шага квиза:
  - Выбор типа практики
  - Выбор длительности
  - Выбор цели
  - Выбор подхода (для медитаций)
  - Выбор объекта концентрации (для самостоятельных медитаций)
- Реализовать навигацию между шагами квиза
- Создать контекст для хранения состояния квиза
- Создать вспомогательные хуки для работы с квизом

### 3. Реализация логики квиза (🔴 Не начато)
- Разработать алгоритм фильтрации практик на основе выбранных параметров
- Реализовать хук для получения данных квиза из Supabase
- Учесть различные ветки выбора (телесные практики, дыхательные, медитации)
- Добавить механизм рандомизации при множественных совпадениях
- Реализовать сохранение истории выборов пользователя

### 4. Разработка плееров (🔴 Не начато)
- Создать видео-плеер для телесных и дыхательных практик
- Создать аудио-плеер для медитаций с сопровождением
- Создать таймер для самостоятельных медитаций
- Реализовать контекст для управления состоянием плееров
- Интегрировать Kinescope для видеоконтента

### 5. Разработка админ-панели (🔴 Не начато)
- Создать защищенный маршрут для админ-панели
- Реализовать интерфейс управления практиками
- Добавить интерфейс для настройки логики квиза
- Создать формы для загрузки медиафайлов
- Реализовать просмотр статистики пользователей

## Детальный план для каждого этапа

### Структура базы данных

1. **Таблица quiz_logic**
   - `id` (UUID, PK)
   - `practice_type` (string) - тип практики: 'physical', 'breathing', 'meditation', 'short'
   - `duration_min` (integer) - минимальная длительность в секундах
   - `duration_max` (integer) - максимальная длительность в секундах
   - `goal` (string) - цель практики, например 'relax', 'energize', etc.
   - `approach` (string) - для медитаций: 'self', 'guided'
   - `content_id` (UUID, FK) - ссылка на таблицу contents
   - `priority` (integer) - приоритет для выбора
   - `created_at`, `updated_at` (timestamptz)

2. **Таблица contents**
   - `id` (UUID, PK)
   - `title` (string)
   - `subtitle` (string)
   - `description` (text)
   - `duration` (integer) - длительность в секундах
   - `thumbnail_url` (string)
   - `background_image_url` (string)
   - `content_type_id` (UUID, FK)
   - `category_id` (UUID, FK)
   - `difficulty_level` (string)
   - `kinescope_id` (string) - для видео
   - `audio_file_path` (string) - для аудио
   - `is_premium` (boolean)
   - `is_featured` (boolean)
   - `display_order` (integer)
   - `metadata` (jsonb)
   - `created_at`, `updated_at` (timestamptz)

3. **Таблица user_quiz_history**
   - `id` (UUID, PK)
   - `user_id` (UUID, FK)
   - `practice_type` (string)
   - `duration` (integer)
   - `goal` (string)
   - `approach` (string)
   - `content_id` (UUID, FK)
   - `completed` (boolean)
   - `created_at` (timestamptz)

### Интерфейс квиза

1. **Компоненты пошагового интерфейса**
   - QuizStart.tsx - начальный экран
   - QuizTypeSelection.tsx - выбор типа практики
   - QuizTimeSelection.tsx - выбор времени
   - QuizGoalSelection.tsx - выбор цели
   - QuizApproachSelection.tsx - выбор подхода (для медитаций)
   - QuizObjectSelection.tsx - выбор объекта (для самостоятельных медитаций)
   - QuizResult.tsx - результат и переход к практике

2. **Контекст для квиза**
   - QuizContext.tsx - хранение состояния квиза
   - useQuizState.ts - хук для работы с состоянием
   - useQuizNavigation.ts - хук для навигации
   - useQuizResults.ts - хук для получения результатов

### Логика квиза

1. **Хуки для работы с данными**
   - useQuizOptions.ts - получение доступных опций для каждого шага
   - useQuizLogic.ts - логика фильтрации практик
   - useQuizHistory.ts - работа с историей выборов

2. **Функции фильтрации**
   - filterByType.ts - фильтрация по типу практики
   - filterByTime.ts - фильтрация по времени
   - filterByGoal.ts - фильтрация по цели
   - getRandomPractice.ts - выбор случайной практики из подходящих

### Система плееров

1. **Компоненты плееров**
   - VideoPlayer.tsx - для телесных и дыхательных практик
   - AudioPlayer.tsx - для медитаций с сопровождением
   - MeditationTimer.tsx - таймер для самостоятельных практик

2. **Контекст для плееров**
   - PlayerContext.tsx - управление состоянием плееров
   - usePlayerControls.ts - управление воспроизведением
   - usePlayerSettings.ts - настройки плеера

### Админ-панель

1. **Компоненты админки**
   - AdminLogin.tsx - форма входа
   - PracticesManagement.tsx - управление практиками
   - QuizLogicEditor.tsx - редактирование логики квиза
   - UserStats.tsx - просмотр статистики

2. **Хуки для админки**
   - useAdminAuth.ts - проверка прав доступа
   - usePracticesManagement.ts - управление практиками
   - useQuizLogicManagement.ts - управление логикой квиза

## Чеклисты для проверки

### Чеклист для квиза
- [ ] Все шаги квиза корректно отображаются
- [ ] Навигация между шагами работает в обе стороны
- [ ] Состояние сохраняется при навигации назад
- [ ] Фильтрация практик работает корректно
- [ ] История выборов сохраняется
- [ ] UI адаптирован под мобильные устройства
- [ ] Соблюдаются гайдлайны Telegram

### Чеклист для базы данных
- [ ] Структура таблиц соответствует требованиям
- [ ] Все необходимые индексы созданы
- [ ] Внешние ключи настроены правильно
- [ ] RLS политики настроены и протестированы
- [ ] Миграции документированы

### Чеклист для плееров
- [ ] Видео корректно воспроизводится
- [ ] Аудио имеет все необходимые элементы управления
- [ ] Таймер работает и уведомляет о завершении
- [ ] Все плееры сохраняют состояние при смене ориентации
- [ ] Полноэкранный режим работает корректно

### Чеклист для админ-панели
- [ ] Доступ защищен авторизацией
- [ ] CRUD операции для практик работают
- [ ] Редактирование логики квиза функционально
- [ ] Загрузка медиафайлов работает
- [ ] Интерфейс интуитивно понятен

## Технические моменты
- Используем React Query для запросов к API
- Используем React Router для навигации
- Реализуем защиту админки через JWT токены
- Храним временно состояние квиза в localStorage
- Используем дебаунсинг для API запросов